<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="estate_property_type_view_form" model="ir.ui.view">
            <field name="name">estate.property.type.form</field>
            <field name="model">estate.property.type</field>
            <field name="arch" type="xml">
                <form string="Estate Property Type">

                    <sheet>
                        <div class="oe_title mb-3">
                            <h1>
                                <field name="name" placeholder="Name"/>
                            </h1>
                        </div>

                        <div class="oe_button_box" name="button_box">
                            <button class="oe_stat_button" name="%(estate_property_offer_action)d" type="action" icon="fa-money" invisible="offer_count == 0">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_value">
                                        <field name="offer_count"/>
                                        <span class="o_stat_text">Offers</span>
                                    </span>
                                </div>
                            </button>
                        </div>
                        <notebook>
                            <page string="Properties">
                                <field name="property_ids">
                                    <!-- Note that it use <list> tag now -->
                                    <list>
                                        <field name="name" string="Title" />
                                        <field name="expected_price" />
                                        <field name="state" string="Status" />
                                    </list>
                                </field>
                            </page>
                        </notebook>

                    </sheet>
                </form>
            </field>
        </record>

        <record id="estate_property_type_view_tree" model="ir.ui.view">
            <field name="name">estate.property.type.list</field>
            <field name="model">estate.property.type</field>
            <field name="arch" type="xml">
                <list string="Estate Property List">
                    <!-- This allows user to drag and drop sorting -->
                    <field name="sequence" widget="handle"/>
                    <field name="name" string="Title"/>
                </list>
            </field>
        </record>
    </data>
</odoo>

