<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <!-- since this will be used as a one2many view, we dont' need menu items and actions -->
        <record id="estate_property_offer_view_tree" model="ir.ui.view">
            <field name="name">estate.property.offer.list</field>
            <field name="model">estate.property.offer</field>
            <field name="arch" type="xml">
                <!-- editable bottom means that new record will go to bottom -->
                <list string="Estate Property Offer List" editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                    <field name="price" />
                    <field name="partner_id" />
                    <field name="validity" />
                    <field name="date_deadline" />
                    <button name="action_accept" icon="fa-check" type="object" invisible="status in ('accepted','refused')" />
                    <button name="action_refuse" icon="fa-times" type="object" invisible="status in ('accepted','refused')"/>
                </list>
            </field>
        </record>



        <record id="estate_property_offer_view_form" model="ir.ui.view">
            <field name="name">estate.property.offer.form</field>
            <field name="model">estate.property.offer</field>
            <field name="arch" type="xml">
                <form string="Estate Offer Form View">
                    <group>
                        <field name="price" />
                        <field name="partner_id" />
                        <field name="validity" />
                        <field name="date_deadline" />
                        <field name="status" />
                    </group>
                </form>
            </field>
        </record>
    </data>
</odoo>

